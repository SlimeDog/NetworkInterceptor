name: ${project.name}
version: ${project.version}
api-version: 1.13
main: ${project.groupId}.${project.artifactId}.bungee.BungeeNetworkInterceptor
description: ${project.description}
author: drives_a_ford, Luck, SlimeDog
load: STARTUP
depend: []
softdepend: [LuckPerms]
